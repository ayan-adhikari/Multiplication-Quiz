# Multiplication quiz
This is website that generates multiplication questions.
You can answer them and click next to generate a new question.
You can run the app from the python script in the app folder (assuming dependencies are installed) or you can run the exe file if you are on windows.
